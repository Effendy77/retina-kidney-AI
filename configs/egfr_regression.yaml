# configs/egfr_regression.yaml

model:
  backbone: vit_base_patch16_224
  dropout: 0.1
  pretrained: true

training:
  batch_size: 16
  epochs: 10
  lr: 1e-4
  weight_decay: 1e-5

data:
  train_csv: data/folds/train_reg.csv
  val_csv: data/folds/val_reg.csv
  image_root: data/images
